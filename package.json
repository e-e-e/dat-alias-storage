{
  "name": "dat-alias-storage",
  "version": "0.0.1",
  "description": "A storage provider for dat that enables aliased file structures.",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "author": "Benjamin Forster",
  "license": "MIT",
  "dependencies": {
    "dat-storage": "^1.1.1",
    "multi-random-access": "^2.1.1",
    "random-access-alias": "0.0.1"
  },
  "devDependencies": {
    "dat-sdk": "^0.3.6",
    "jest": "^24.9.0",
    "rimraf": "^3.0.0"
  }
}
